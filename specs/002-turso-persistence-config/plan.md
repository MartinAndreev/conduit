# Plan

1. Validate embedded `@tursodatabase/database` in Node and the Bun standalone binary.
2. Add global and project database schemas, migrations, startup coordination, and backup/migration from existing JSON state.
3. Add global profile repositories and configuration resolution.
4. Migrate drafts, refinement revisions, run events, transcripts, diffs, and reviews behind domain repositories.
5. Write final story/packet files only after explicit packet approval.
