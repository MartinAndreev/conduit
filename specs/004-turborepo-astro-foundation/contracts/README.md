# Contracts

The workspace package boundaries are:

- `packages/conduit`: the publishable CLI and standalone release artifact.
- `packages/website`: a private static Astro site deployed to GitHub Pages.

No runtime code or domain contracts are shared between these packages in this
packet.
