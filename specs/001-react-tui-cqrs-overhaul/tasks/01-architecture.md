# Group 1 — Architecture documents and agent rules

## Scope

Create the human and agent implementation contract only. Do not migrate source code, dependencies, commands, or tests in this group.

## Tasks

- [x] Document layers, CQRS ownership, feature lifecycle, runner normalization, provider/security model, TUI screens, keyboard conventions, and theme tokens.
- [x] Add `AGENTS.md` with scope boundaries, file organization, secret handling, verification, and no-commit rules for implementation agents.
- [x] Add this complete feature packet and task groups 01–05.

## Acceptance criteria

- [x] A new agent can identify where a view, handler, provider, runner adapter, type, enum, interface, and helper belongs without guessing.
- [x] Documents state all agreed product decisions and identify remote providers as future scope.
- [x] No production source or runtime behavior changes.

## Review record

Accepted by the architect after an OpenCode documentation pass and formatting verification. No runtime source, dependency, workflow, or CLI changes were made.
